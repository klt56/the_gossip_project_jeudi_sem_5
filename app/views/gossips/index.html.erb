<p><h1>gossips</h1></p>
<br>

<% @gossips.each do |gossip|%>
<h2><%= gossip.title %></h2>
<p><%= gossip.content %></p>
<%= link_to "voir les gossip", gossip_path(gossip) %>
<p>
<a href="<% gossip_path(gossip.id) %>" class="btn btn-primary">lire la suite</a>
</p>
<% end %>